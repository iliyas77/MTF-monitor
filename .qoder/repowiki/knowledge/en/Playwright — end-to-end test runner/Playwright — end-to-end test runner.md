---
kind: external_dependency
name: Playwright — end-to-end test runner
slug: playwright
category: external_dependency
category_hints:
    - vendor_identity
scope:
    - '**'
---

### Role
- Declared as a `devDependency` in `package.json`; used for browser-based e2e tests against the locally served app (via `http-server -p 8080`).